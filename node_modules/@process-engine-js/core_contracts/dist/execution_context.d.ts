import { TokenType, ISessionStoreData, IIamService, IIdentity } from './interfaces';
export declare class ExecutionContext {
    sessionData: any;
    tokenType: TokenType;
    encryptedToken: any;
    entityCallStack: any;
    private _iamService;
    private _promotions;
    constructor(iamService: IIamService, sessionData: ISessionStoreData, tokenType: TokenType, encryptedToken: any);
    private readonly iamService;
    readonly isGuest: boolean;
    hasRole(role: string): boolean;
    hasRoles(rolesToCheck: Array<string>): boolean;
    promoteWith(role: string): string;
    unpromote(promoteContext: string): void;
    getRoles(): Array<string>;
    getIdentity(context: ExecutionContext): Promise<IIdentity>;
    private _getIdentityRoles();
}
