'use strict';

const MetadataProvider = require('./dist/commonjs/index').MetadataProvider;

function registerInContainer(container) {

  container.registerObject('MetadataProvider', MetadataProvider);
}


module.exports.registerInContainer = registerInContainer;
